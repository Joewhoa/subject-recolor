# 历史参考（不要作为主入口）

这里用于说明历史方案，而不是让用户日常运行。

- 旧的临时curl命令已证明该部署的网关兼容curl；
- 旧httpx工具有完整架构，但某次部署观察到它与curl存在兼容性差异（httpx快速空体503，这不等同于“httpx必然失败”）；
- 正式版已把两者合并：保留Python任务架构，传输改为system curl，作为该部署的验证适配器。

新Agent不要为了“保留两套”而恢复第二个正式入口。发生故障时以`验证记录/`和`文档/05_技术方案与验证结论.md`做回归依据。

# Subject Recolor 维护与发布交接说明

日期：2026-09-02

## 当前结论

本轮维护已完成本地文档、行为、测试、构建、清单和敏感信息核验，并将项目定位为**主体换色**（任意主体，如窗帘/沙发/雨伞等）——「户外窗帘」仅是现场交付层的默认示例主体，不是项目身份。README 已按 dsh-roam 风格收敛为务实的工程与生产交付说明。

仓库根目录代表较早、可复现的工程基线。`新方案交接落地/` 是后续生产交付演进：其直接背景是非技术用户难以独立操作原 CLI，因此增加了 Agent 操作方式、面向初学者的文档、单图单色一键入口、可恢复批处理、付费调用安全状态机、严格缓存和人工复核。

## 已确认的生产行为

- B 为默认方案。
- A 仅用于用户明确指定的逐项复核；系统不会自动执行 A，也绝不会自动执行 A+B。
- 503 进入 `uncertain`，禁止自动重试。
- 仅 429 和能够确认“连接尚未建立”的错误允许有界重试，最多一次；`max_safe_retries` 仅允许 `0` 或 `1`。
- curl 本地启动失败属于本地致命错误，不重试。
- request ID 只用于追踪，不提供幂等保证。
- 单次入口发现同一任务身份已有产物或 attempt sidecar 时拒绝重跑；用户人工检查后必须指定新的 `--out`。
- 批处理跨运行重试 `uncertain` 项时，attempt 序号和 request ID 保持单调递增，不会覆盖旧记录。
- API key 仅通过环境变量注入，不进入 Git、日志、输出、聊天或 curl argv；curl 通过短生命周期、私有权限的临时 header 文件接收凭据，并尽力删除该文件。
- 批处理仅在任务身份及规定的产物哈希均严格匹配时命中缓存。
- 生成结果仍必须经过人工复核后才能接受或继续处理。

## 本轮验证结果

验证日期为 2026-09-02，环境为 Python 3.14.7、curl 8.21.0。

- 根目录工程基线：34 个 pytest 测试通过，覆盖率 83%。
- 根目录工程基线：Ruff 通过。
- 现场批处理交付：13 个测试通过，其中包括使用真实系统 curl 对 localhost 假 API 的测试。
- 单次入口：3 个测试通过。
- 环境检查通过。
- 本地 `dist/` wheel 曾重建为 27,975 字节、SHA-256 `BC8166DD35050DB76E826598566D9AC6B87DF8B2D8A99232A26A9A0462D3B2D9`（与过期哈希不同）；但 README 开篇随后又做了主体换色定位收敛，发布 Release 附加 wheel 前需再次重建。
- 现场交付 `SHA256SUMS.txt` 共 48 项，全部校验一致。
- `文件清单.txt` 恰好列出相同的 48 项。
- 现场交付中的 `__pycache__` 和 `.pyc` 已清除。

## 敏感信息与发布卫生

- 当前候选文件扫描未发现 RFC1918 内网端点、真实 request ID、字面量 Bearer 凭据或内部 billing/usage 信息。
- 忽略的缓存、生成输出和本地配置不纳入发布候选。
- 本文不复述任何 request ID 或秘密信息。

## 历史处置与发布状态

- 敏感历史已处置：唯一提交已通过 `git commit --amend` 重写，并用 `git push --force-with-lease origin main` 强推（`7998938..1a03648`，随后 `1a03648..25218d0`）。可达历史不再包含那两个真实 request ID 或内部账本叙述；本地 reflog 在 GC 前可能仍引用旧提交，GitHub 侧不可达对象会另行过期。
- GitHub Actions 已远端核验：最新提交 `25218d0` 的 CI run 为 `success`（Python 3.11/3.12/3.13 矩阵）。
- 仓库 About 描述与 topics 已设置：描述为主体换色一句话简介；topics 为 `image-editing`、`python`、`generative-ai`、`batch-processing`、`computer-vision`、`dsh`。
- `dist/` 被忽略，wheel 不纳入版本控制；发布 Release 附加 wheel 前需重建（README 开篇在最后一次重建后又做了主体换色定位收敛）。
- GitHub Release/tag 尚未创建，发布 Release 仍属所有者决定。
- 本轮已执行 amend 重写与 force-push；未创建仓库、未创建 Release。

# 正式交接版验证报告

验证日期：2026-09-02

## 已执行且通过

1. 环境自检：Python 3.12.14、curl 8.7.1、Pillow 12.3.0、核心文件完整；
2. Python语法编译：`main.py`、环境检查和全部recolor模块通过；
3. 历史交接时离线回归测试9项通过；公开整理版现为批量13项＋一次性3项通过；
4. 真实system-curl本机集成测试：启动localhost假API，真正执行curl multipart，检查model/prompt/image/response_format和request ID，返回假Base64 PNG并由客户端解析，通过；
5. 用正式交接工具对0901运行`--dry-run`：识别7张原图、1张色卡、7个任务，固定单线程，色值`#5F5B58 RGB(95,91,88)`，通过；
6. 正式工具不再依赖httpx，`requirements.txt`仅包含Pillow；
7. `config.example.json`和代码默认`context_workers=1`，CLI只接受`--workers 1`。

## 离线测试明细

- 中心200×200取色和B prompt；
- 笛卡尔积、context和checkpoint；
- 压缩不修改原图；
- PNG母版和JPG修复；
- 假API端到端及断点跳过；
- uncertain立即停止；
- 多原图上下文顺序隔离/切换；
- curl成功JSON/Base64解析；
- curl空体503默认按uncertain处理（不自动重试）。

## 未在交接版重复执行的付费测试

交接版完成后没有再调用付费API，避免无必要费用。curl传输可用性已由此前三次真实付费/实际端点成功请求证明，摘要存放在本目录JSON文件：

- 1254历史样例：HTTP 200，77.9秒；
- 0901原始4K＋B：HTTP 200，60.7秒；
- 0901压缩2048缓存＋B：HTTP 200，32.66秒。

新Agent首次生产使用仍必须按：dry-run → prepare-only → limit 1 → 用户审阅 → 全量。

## 安全检查

- 交接包不包含API Key；
- 不包含Base64响应正文；
- 不包含虚拟环境和图片大文件；
- Key通过权限受限的临时header文件交给curl，不进入子进程argv；
- 临时API响应在解析后删除；
- 输出/checkpoint采用原子写入。
